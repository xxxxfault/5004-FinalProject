package final_project_HITPLANE;

/**
 * Create an enum for the state of the flyer object
 * there are 3 states of every flyer object: live, dead, remove
 */
public enum FlyerState {
    Live,Dead,Remove
}
